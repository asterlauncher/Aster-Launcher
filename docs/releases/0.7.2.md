# Aster Launcher 0.7.2

- Uses `synoi` as the protected Aster owner name instead of `synoi_`.
- Updates the owner-only settings visibility and BTD6 moderation labels.
- Keeps Supabase as the final authority for gift and moderation actions.
